package ec.app.tutorial4.nodes.terminal;

import ec.app.tutorial4.OPNode;
import ec.gp.GPNode;

public abstract class Stochastic_Terminal extends GPNode{

//	@Override
//	public Object clone() {
//
//		GPNode newnode = (GPNode)(lightClone());
//		OPNode2 node = (OPNode2) newnode.parent;
//		node.reset();
//        for(int x=0;x<children.length;x++)
//        {
//        	newnode.children[x] = (GPNode)(children[x].cloneReplacing()); 
//            newnode.children[x].parent = newnode;
//            newnode.children[x].argposition = (byte)x;
//        }
//        return newnode;
//	}
}
